module register #(parameter N=8) (input clk, rst,
											 input [N-1:0] D,
											 input [N-1:0] Q)
											 
	always_ff @(posedge clk or posedge rst)
		if (rst) Q = 'h0;
		else
			Q = D;
			
endmodule